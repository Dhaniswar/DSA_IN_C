#include<stdio.h>

/*
1). Circular Linked list

=> It is the variation of singly linked list

=> only difference in circular linked list is that it will store address of first node in the address part of last node

=> Circular linked list is the sequence of elements in which each element is having link to it's next element and last element is having linked to the first element in the list


*/

struct node{
    int a;
    struct node *next;
};


int main() {

    return 0;
}